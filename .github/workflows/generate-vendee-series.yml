name: Generate Vendée historical series

# Reserved manual workflow. Production currently uses the proven batch renderer.
on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  manual-note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use the proven production batch workflow for reviewed episodes."
