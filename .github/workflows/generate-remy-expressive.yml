name: Generate expressive Remy variants

on:
  workflow_dispatch:

# Kept only for manual experiments. Automated expressive generation now lives
# in generate-edge-audio.yml to avoid concurrent jobs pushing to main.
permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use generate-edge-audio.yml for automated expressive generation."
